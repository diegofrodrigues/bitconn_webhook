<odoo>
  <data>
    <!-- Donate Modal View (defined first so button reference works) -->
    <record id="view_bitconn_webhook_donate_modal" model="ir.ui.view">
      <field name="name">bitconn.webhook.donate.modal</field>
      <field name="model">ir.actions.act_window</field>
      <field name="arch" type="xml">
        <form string="Support / Apoiar">
          <group>
            <group>
              <p>
                <span>
                  If this module helped you, consider a donation (or buy me a beer 🍺) to keep development active!
                </span>
                <br/>
                <span style="display:block;border-top:1px solid #ddd;margin:10px 0 8px 0;"></span>
                <span>
                  Se este módulo foi útil, considere fazer uma doação (ou pagar uma cerveja 🍺) para manter o desenvolvimento!
                </span>
                <br/><br/>
                <strong>Pix Key / Chave Pix:</strong>
                <br/>
                <span style="font-size:12px; word-break:break-all; display:inline-block; margin-top:4px;">00020126810014br.gov.bcb.pix013655f22863-4cea-41e9-904c-df3ce0b241ef0219wa conn odoo module5204000053039865802BR5924Diego Ferreira Rodrigues6009Sao Paulo62290525REC68545B90764819659464106304D86E</span>
              </p>
            </group>
            <group>
              <div style="text-align:center; margin-top:10px;">
                <img src="/bitconn_webhook/static/description/qr_code_donate.png" style="max-width:200px;display:block;margin:auto;" alt="Donate QR"/>
                <span id="pix-feedback-bw" style="display:none;color:green;font-weight:bold;margin-left:10px;">Copied!</span>
                <br/>
                <a href="#" class="btn btn-secondary" role="button"
                   onclick="navigator.clipboard.writeText('00020126810014br.gov.bcb.pix013655f22863-4cea-41e9-904c-df3ce0b241ef0219wa conn odoo module5204000053039865802BR5924Diego Ferreira Rodrigues6009Sao Paulo62290525REC68545B90764819659464106304D86E').then(function(){var m=document.getElementById('pix-feedback-bw');if(m){m.style.display='inline';setTimeout(function(){m.style.display='none';},2000);}}); return false;">
                  Copy Pix Key
                </a>
              </div>
            </group>
          </group>
          <footer>
            <button string="Close" class="btn-secondary" special="cancel"/>
          </footer>
        </form>
      </field>
    </record>

    <record id="bitconn_webhook_donate_modal_action" model="ir.actions.act_window">
      <field name="name">Donate</field>
      <field name="res_model">ir.actions.act_window</field>
      <field name="view_mode">form</field>
      <field name="view_id" ref="view_bitconn_webhook_donate_modal"/>
      <field name="target">new</field>
    </record>

    <record id="view_bitconn_webhook_form" model="ir.ui.view">
      <field name="name">bitconn.webhook.form</field>
      <field name="model">bitconn.webhook</field>
      <field name="arch" type="xml">
  <form>
          <header>
            <button name="%(bitconn_webhook_donate_modal_action)d" type="action" string="Donate" class="btn-donate" icon="fa-money"/>
            <style>
              .btn-donate {
                animation: bounce 1s ease 0s 5;
                background-color: #4285f4 !important;
                color: #ffffff !important;
              }
              @keyframes bounce {
                0%,100% { transform: translateY(0); }
                20% { transform: translateY(-18px); }
                40% { transform: translateY(0); }
                60% { transform: translateY(-9px); }
                80% { transform: translateY(0); }
              }
            </style>
          </header>
          <sheet>
            <group col="2">
              <field name="name"/>
              <field name="user_id" options="{'no_create': True, 'no_open': True}"/>
              <field name="can_create" invisible="can_code == True"/>
              <field name="can_write" invisible="can_code == True"/>
              <field name="can_unlink" invisible="can_code == True"/>
              <field name="can_code" invisible="can_create == True or can_write == True or can_unlink == True"/>
            </group>
            <notebook>
              <page string="Inbound">
                <!-- Warning message when can_code is True -->
                <group invisible="can_code == False">
                  <div class="alert alert-warning" role="status">
                    <strong>Warning:</strong> Custom code execution is a powerful feature. Only enable for trusted sources.
                    The code will run with the configured user's permissions and full access to all models.
                  </div>
                </group>
                <div class="alert alert-danger" role="alert" invisible="inbound_auth_type != 'none'">
                  <strong>⚠️ Security Warning:</strong> This webhook is configured for public access without authentication.
                  Anyone with the webhook URL can send requests. Only use this for trusted sources or public endpoints.
                </div>
                
                <group>
                  <group col="2" class="w-100">
                    <field name="allowed_model_ids" widget="many2many_tags" invisible="can_code == True"/>
                    <field name="inbound_auth_type" widget="radio"/>
                  </group>
                  <group col="2" class="w-100">
                    <field name="webhook_url" readonly="1" widget="CopyClipboardURL"/>
                    <field name="secret_key" readonly="1" widget="CopyClipboardChar" invisible="inbound_auth_type == 'none'"/>
                    <!-- <field name="webhook_uuid" readonly="1"/> -->
                  </group>
                </group>
                <group invisible="inbound_auth_type == 'none'">
                    <button name="action_regenerate_credentials" string="Regenerate Credentials" type="object" icon="fa-refresh" class="btn-link d-block mx-auto" colspan="2"/>
                </group>
                
                <!-- Python Code Section (visible only when can_code is True) -->
                <separator string="HTTP Methods" invisible="can_code == False"/>
                <group col="2" invisible="can_code == False">
                  <field name="inbound_allowed_methods" widget="radio"/>
                  <group colspan="2" invisible="inbound_allowed_methods != 'CUSTOM'">
                    <group>
                      <field name="inbound_methods_get"/>
                      <field name="inbound_methods_post"/>
                      <field name="inbound_methods_put"/>
                    </group>
                    <group>
                      <field name="inbound_methods_patch"/>
                      <field name="inbound_methods_delete"/>
                    </group>
                  </group>
                </group>
                
                <separator string="Python Code" invisible="can_code == False"/>
                <div class="text-muted mb-2" invisible="can_code == False">
                  Write your Python code below. Set <code>result</code> variable to return custom response.
                  <br/>
                  <strong>💡 Tip:</strong> See the <strong>Help</strong> tab for complete documentation and available variables.
                </div>
                <field name="python_code" nolabel="1" widget="code" options="{'mode': 'python', 'lineWrapping': true, 'lineNumbers': true}" invisible="can_code == False" placeholder="# Available variables: request, env, user_id, json&#10;# Set result variable to return response&#10;&#10;# Example: Create a partner from webhook data&#10;data = request['json']&#10;partner = env['res.partner'].create({&#10;    'name': data.get('name'),&#10;    'email': data.get('email')&#10;})&#10;result = {'ok': True, 'partner_id': partner.id, 'name': partner.name}"/>
                
                <separator string="Request Monitor (Real-time)" invisible="can_code == False"/>
                <div class="text-muted mb-2" invisible="can_code == False">
                  These fields update automatically when a webhook request is received while you're on this tab.
                </div>
                <group col="2" class="o_group o_inner_group" invisible="can_code == False">
                  <group class="w-100">
                    <label for="last_request_input" string="Last Request Input" class="o_form_label"/>
                    <field name="last_request_input" nolabel="1" widget="code" options="{'mode': 'javascript', 'lang': 'json', 'lineWrapping': true, 'lineNumbers': true, 'readonly': true}" readonly="1"/>
                    <field name="last_request_date" readonly="1"/>
                  </group>
                  <group class="w-100">
                    <label for="last_request_output" string="Last Request Output" class="o_form_label"/>
                    <field name="last_request_output" nolabel="1" widget="code" options="{'mode': 'javascript', 'lang': 'json', 'lineWrapping': true, 'lineNumbers': true, 'readonly': true}" readonly="1"/>
                  </group>
                </group>
                <div class="d-flex align-items-center mb-3" invisible="can_code == False">
                  <button name="action_refresh_monitor" type="object" string="Refresh Monitor" class="btn-primary me-2" icon="fa-refresh"/>
                  <button name="action_clear_request_input" type="object" string="Clear Monitor" class="btn-secondary" icon="fa-eraser"/>
                </div>
                
                <separator string="Test Code" invisible="can_code == False"/>
                <group col="1" invisible="can_code == False">
                  <field name="enable_test"/>
                </group>
                <div invisible="can_code == False or enable_test == False or pin_request == True">
                  <div class="text-muted mb-2">Simulate the JSON request body that will be sent to the webhook to test your Python code.</div>
                  <field name="test_input" nolabel="1" widget="code" options="{'mode': 'javascript', 'lang': 'json', 'lineWrapping': true, 'lineNumbers': true}"/>
                </div>
                <div class="d-flex align-items-center mb-3" invisible="can_code == False or enable_test == False">
                  <button name="action_test_code" type="object" string="Test Code" class="btn-primary me-2" icon="fa-play"/>
                  <button name="action_clear_test_output" type="object" string="Clear Output" class="btn-secondary" icon="fa-eraser"/>
                </div>
                <div invisible="can_code == False or enable_test == False">
                  <label for="test_output" string="Test Output (Result Variable)" class="o_form_label"/>
                  <field name="test_output" nolabel="1" widget="code" options="{'mode': 'javascript', 'lang': 'json', 'lineWrapping': true, 'lineNumbers': true, 'readonly': true}" readonly="1"/>
                </div>
                
                <separator string="Sample Request (Latest Received)" invisible="can_code == False"/>
                <div invisible="can_code == False">
                  <field name="pin_request"/>
                  <div class="text-muted mb-2" invisible="pin_request == False">
                    <i class="fa fa-thumb-tack"/> Using this sample as test input. Uncheck above to use custom Test Input.
                    <br/>
                    <strong>Note:</strong> Webhook responses will include author/support information when Pin Request is enabled.
                  </div>
                </div>
                <div invisible="can_code == False or pin_request == False">
                  <field name="sample_request_payload" nolabel="1" widget="code" options="{'mode': 'javascript', 'lang': 'json', 'lineWrapping': true, 'lineNumbers': true, 'readonly': true}" readonly="1"/>
                </div>
                <div class="d-flex align-items-center mb-3" invisible="can_code == False or pin_request == False">
                  <button name="action_refresh_sample" type="object" string="Refresh Sample" class="btn-primary me-2" icon="fa-refresh"/>
                  <button name="action_clear_sample_request" type="object" string="Clear Sample" class="btn-secondary" icon="fa-eraser"/>
                </div>
                
              </page>

              <page string="Outbound">
                <group col="2">
                  <group>
                    <field name="outbound_enabled"/>
                  </group>
                  <group>
                    <field name="outbound_method"/>
                  </group>
                </group>
                <group col="1">
                  <field name="outbound_url"/>
                </group>
                <separator string="Headers"/>
                <field name="outbound_headers" nolabel="1" widget="code" options="{'mode': 'javascript', 'lang': 'json', 'lineWrapping': true, 'lineNumbers': true}" placeholder='{"Authorization": "Bearer xxxxxxxxxxx"}'/>
                <!-- Automation Rules - fora do group para largura total -->
                <separator string="Automation Rules"/>
                <div class="mb-4">
                  <button name="action_create_server_action" type="object" string="Adicionar" class="btn-primary" icon="fa-plus"/>
                </div>
                <field name="automation_ids" nolabel="1" widget="one2many" class="o_base_automation_actions_field" create="false" delete="false"
                      context="{'active_test': False}">
                  <kanban class="o_base_automation_kanban_view" records_draggable="false" quick_create="false" group_create="false" create="false">
                    <control>
                    </control>
                    <field name="active"/>
                    <templates>
                      <t t-name="card" class="flex-md-row">
                        <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger" invisible="active" />
                        <div class="d-flex align-items-center w-100 w-md-25 o_automation_base_info">
                          <div class="d-flex flex-column">
                            <field name="name" class="fs-2 fw-bold"/>
                            <field name="model_id" invisible="context.get('default_model_id')"/>
                            <div class="d-flex align-items-center gap-1 text-nowrap" invisible="trigger in ['on_time', 'on_time_created', 'on_time_updated']">
                              <field name="trigger"/>
                              <field name="on_change_field_ids" invisible="trigger != 'on_change'" class="my-1" />
                              <field name="trg_selection_field_id" invisible="trigger not in ['on_state_set', 'on_priority_set']" class="o_tag o_tag_color_0 rounded-pill p-1 px-2" />
                              <field name="trg_field_ref" invisible="trigger not in ['on_stage_set', 'on_tag_set']" class="o_tag o_tag_color_0 rounded-pill p-1 px-2" />
                              <field name="trigger_field_ids" invisible="trigger not in ['on_create_or_write']" class="my-1" />
                            </div>
                          </div>
                        </div>
                        <div class="d-none d-md-flex flex-grow-1 align-items-center gap-3 o_automation_actions" data-name="more-info">
                          <i class="fa fa-2x fa-arrow-right text-primary" title="Actions" />
                          <field name="action_server_ids" widget="base_automation_actions_one2many" class="align-self-center w-100 me-md-3" />
                        </div>
                      </t>
                    </templates>
                  </kanban>
                </field>
                <separator string="Test"/>
                <group col="1">
                  <field name="enable_test_outbound"/>
                </group>
                <group col="1" invisible="enable_test_outbound == False">
                  <label for="outbound_test_body" string="Test Body (JSON)" class="o_form_label"/>
                  <field name="outbound_test_body" nolabel="1" widget="code" options="{'mode': 'javascript', 'lang': 'json', 'lineWrapping': true, 'lineNumbers': true}"/>
                </group>
                <group col="1" invisible="enable_test_outbound == False">
                  <div class="d-flex align-items-center">
                    <button name="action_test_outbound" type="object" string="Send Test" class="btn-primary me-2" icon="fa-paper-plane"/>
                    <button name="action_clear_outbound_test_result" type="object" string="Clear Test Result" class="btn-secondary" icon="fa-eraser"/>
                  </div>
                </group>
                <group col="1" invisible="enable_test_outbound == False">
                  <label for="outbound_test_result" string="Test Result" class="o_form_label"/>
                  <field name="outbound_test_result" nolabel="1" widget="code" options="{'mode': 'javascript','lang': 'json', 'lineWrapping': true, 'lineNumbers': true, 'readonly': true}" readonly="1"/>
                </group>
              </page>
              <page string="Terminal">
                <group>
                  <div class="o_bitconn_terminal_container" colspan="2">
                    <div id="bitconn_terminal_banner" class="alert alert-warning" style="display:none;margin-bottom:8px;" role="alert">Session expired. Click <strong>Open Shell</strong> to start a new session.</div>
                    <div id="bitconn_terminal" class="xterm"></div>
                    <div class="d-flex mt-2">
                      <a href="#" role="button" class="btn btn-success o_bitconn_terminal_connect">Connect</a>
                      <a href="#" role="button" class="btn btn-primary ms-2 o_bitconn_terminal_shell">Shell</a>
                      <a href="#" role="button" class="btn btn-danger ms-2 o_bitconn_terminal_disconnect" style="display:none;">Disconnect</a>
                      <a href="#" role="button" class="btn btn-secondary ms-2 o_bitconn_terminal_clear">Clear</a>
                    </div>
                  </div>
                </group>
              </page>

              <page string="Help">
                <group>
                  <separator string="Python Code - Available Variables"/>
                </group>
                <group>
                  <div class="alert alert-info" role="status">
                    <h4>📚 Python Code Documentation</h4>
                    <p>When custom code execution is enabled, you have access to the following variables in your Python code:</p>
                    
                    <h5><code>request</code> - Request object</h5>
                    <ul>
                      <li><code>request['body']</code> - Raw request body as string. Use for signature validation, XML parsing, or when you need the original text.</li>
                      <li><code>request['json']</code> - Parsed JSON body as Python dict. Most convenient for accessing request data.</li>
                      <li><code>request['headers']</code> - Request headers as dict. Access with <code>request['headers'].get('Content-Type')</code></li>
                      <li><code>request['method']</code> - HTTP method (GET, POST, PUT, PATCH, DELETE). Use to route different logic based on method.</li>
                    </ul>
                    
                    <h5><code>env</code> - Odoo Environment</h5>
                    <ul>
                      <li>Full Odoo environment with configured user permissions</li>
                      <li>Access any model: <code>env['res.partner']</code>, <code>env['sale.order']</code>, etc.</li>
                      <li>Execute CRUD operations: <code>.create()</code>, <code>.search()</code>, <code>.write()</code>, <code>.unlink()</code></li>
                    </ul>
                    
                    <h5><code>user_id</code> - Configured User ID</h5>
                    <ul>
                      <li>ID of the user configured in the webhook (integer)</li>
                      <li>Useful for logging or filtering: <code>env['res.users'].browse(user_id)</code></li>
                    </ul>
                    
                    <h5><code>json</code> - JSON Module</h5>
                    <ul>
                      <li>Python's json module for parsing/dumping</li>
                      <li><code>json.loads()</code>, <code>json.dumps()</code></li>
                    </ul>
                    
                    <h5><code>result</code> - Return Variable ⚠️</h5>
                    <ul>
                      <li><strong>Important:</strong> Set this variable to define the webhook response</li>
                      <li>Must be a dict: <code>result = {'ok': True, 'message': 'Success'}</code></li>
                      <li>Will be returned as JSON to the webhook caller</li>
                      <li>If not set, an error message will be returned</li>
                    </ul>
                    
                    <h5>💡 Example - Filter by HTTP Method</h5>
                    <pre><code>if request['method'] == 'GET':
    # Handle GET - return data
    partners = env['res.partner'].search([('customer_rank', '>', 0)])
    result = {'ok': True, 'count': len(partners), 'partners': partners.mapped('name')}

elif request['method'] == 'POST':
    # Handle POST - create record
    data = request['json']
    partner = env['res.partner'].create({'name': data.get('name'), 'email': data.get('email')})
    result = {'ok': True, 'id': partner.id, 'created': True}

elif request['method'] == 'DELETE':
    # Handle DELETE - remove record
    partner_id = request['json'].get('id')
    partner = env['res.partner'].browse(partner_id)
    partner.unlink()
    result = {'ok': True, 'deleted': True}

else:
    result = {'ok': False, 'error': 'method_not_supported'}</code></pre>
                  </div>
                </group>
                
                <group>
                  <separator string="CRUD Operations - Inbound Webhook"/>
                </group>
                <group>
                  <div class="alert alert-info" role="status">
                    <h4>📥 Inbound CRUD Operations</h4>
                    <p>When custom code execution is <strong>disabled</strong>, the webhook accepts standard CRUD operations via JSON payload.</p>
                    
                    <h5>📝 Create - Insert New Records</h5>
                    <p><strong>Method:</strong> <code>"method": "create"</code></p>
                    <p><strong>Use case:</strong> External system sends data to create a new record in Odoo</p>
                    <p><strong>Required:</strong> <code>model</code> and <code>values</code> fields</p>
                    <p><strong>Returns:</strong> <code>{'ok': True, 'id': &lt;record_id&gt;}</code></p>
                    <pre><code>{
  "model": "res.partner",
  "method": "create",
  "values": {
    "name": "New Partner",
    "email": "partner@example.com",
    "phone": "+5511999999999"
  }
}</code></pre>
                    
                    <h5>✏️ Write - Update Existing Records</h5>
                    <p><strong>Method:</strong> <code>"method": "write"</code></p>
                    <p><strong>Use case:</strong> Update records matching a domain filter</p>
                    <p><strong>Required:</strong> <code>model</code>, <code>domain</code>, and <code>values</code></p>
                    <p><strong>Returns:</strong> <code>{'ok': True, 'ids': [...]}</code></p>
                    <pre><code>{
  "model": "res.partner",
  "method": "write",
  "domain": [["email", "=", "partner@example.com"]],
  "values": {
    "comment": "Updated via webhook"
  }
}</code></pre>
                    
                    <h5>🗑️ Unlink - Delete Records</h5>
                    <p><strong>Method:</strong> <code>"method": "unlink"</code></p>
                    <p><strong>Use case:</strong> Delete records matching a domain (destructive!)</p>
                    <p><strong>Required:</strong> <code>model</code> and <code>domain</code></p>
                    <p><strong>Security:</strong> Must enable "Can Unlink" permission</p>
                    <p><strong>Returns:</strong> <code>{'ok': True, 'ids': [...]}</code></p>
                    <pre><code>{
  "model": "res.partner",
  "method": "unlink",
  "domain": [["id", "in", [10, 11]]]
}</code></pre>
                    
                    <h5>🔍 Search - Find and Read Records</h5>
                    <p><strong>Method:</strong> <code>"method": "search"</code></p>
                    <p><strong>Use case:</strong> Query records and return structured data with nested relations</p>
                    <p><strong>Required:</strong> <code>model</code></p>
                    <p><strong>Optional:</strong> <code>domain</code>, <code>fields</code>, <code>limit</code>, <code>offset</code>, <code>order</code></p>
                    <p><strong>Advanced:</strong> Use dict spec in <code>fields</code> for nested relations</p>
                    <p><strong>Returns:</strong> <code>{'ok': True, 'count': N, 'records': [...]}</code></p>
                    <pre><code>{
  "model": "sale.order",
  "method": "search",
  "domain": [["state", "=", "sale"]],
  "limit": 2,
  "fields": [
    "id",
    "name",
    {"partner_id": ["id", "name", "email"]},
    {"order_line": [
      "id",
      "name",
      "product_uom_qty",
      {"product_id": ["id", "default_code", "name"]}
    ]}
  ]
}</code></pre>
                    
                    <h5>📖 Read - Get Specific Records by ID</h5>
                    <p><strong>Method:</strong> <code>"method": "read"</code></p>
                    <p><strong>Use case:</strong> Fetch specific records by their IDs</p>
                    <p><strong>Required:</strong> <code>model</code> and <code>ids</code></p>
                    <p><strong>Optional:</strong> <code>fields</code> (same advanced spec as search)</p>
                    <p><strong>Returns:</strong> <code>{'ok': True, 'records': [...]}</code></p>
                  </div>
                </group>
                
                <group>
                  <separator string="Server Actions - Outbound Webhook"/>
                </group>
                <group>
                  <div class="alert alert-info" role="status">
                    <h4>📤 Outbound Webhooks via Server Actions</h4>
                    <p>Send data from Odoo to external systems using Server Actions with Bitconn Webhook integration.</p>
                    
                    <h5>🎯 How it Works</h5>
                    <ul>
                      <li><strong>Trigger:</strong> Automation rules, manual execution, or scheduled actions</li>
                      <li><strong>Action Type:</strong> Select "Bitconn Webhook" in Server Action</li>
                      <li><strong>Configuration:</strong> Choose webhook, select fields, or write custom Python payload</li>
                      <li><strong>Execution:</strong> Data is sent via HTTP request to configured URL</li>
                    </ul>
                    
                    <h5>🔧 Configuration Modes</h5>
                    
                    <h6>1️⃣ Field Selection (Simple)</h6>
                    <p>Select which fields to include in the payload. Good for straightforward data export.</p>
                    <ul>
                      <li>Choose fields from the model</li>
                      <li>System automatically builds JSON payload</li>
                      <li>Supports simple fields only (no nested relations in UI)</li>
                    </ul>
                    
                    <h6>2️⃣ Manual Payload (Advanced)</h6>
                    <p>Write JSON structure manually with placeholder support for dynamic values.</p>
                    <ul>
                      <li><strong>Placeholders:</strong> Use <code>{{field_name}}</code> for simple fields</li>
                      <li><strong>Relations:</strong> Use <code>{{relation.field}}</code> for nested data</li>
                      <li><strong>Advanced Spec:</strong> Use <code>"fields": [...]</code> with dict objects for nested relations (same as search)</li>
                    </ul>
                    <pre><code>{
  "customer": "{{name}}",
  "email": "{{email}}",
  "order_ref": "{{order_id.name}}",
  "status": "active"
}</code></pre>
                    
                    <h6>3️⃣ Python Payload (Maximum Flexibility)</h6>
                    <p>Write Python code to build custom payloads with business logic.</p>
                    <ul>
                      <li><strong>Variables:</strong> <code>record</code>, <code>records</code>, <code>env</code></li>
                      <li><strong>Return:</strong> Set <code>result</code> variable with your payload dict</li>
                      <li><strong>Logic:</strong> Add conditions, calculations, API calls, etc.</li>
                    </ul>
                    <pre><code># Build custom payload with logic
total = sum(record.order_line.mapped('price_total'))

result = {
    "order_id": record.name,
    "customer": record.partner_id.name,
    "total": total,
    "currency": record.currency_id.name,
    "status": "approved" if total > 1000 else "pending"
}</code></pre>
                    
                    <h5>📊 Response Handling</h5>
                    <p>After execution, check the <strong>Last Result</strong> field (JSON format):</p>
                    <pre><code>{
  "url": "https://api.example.com/webhook",
  "status": 200,
  "request": { /* your payload */ },
  "response": { /* API response */ }
}</code></pre>
                    
                    <h5>💡 Use Cases</h5>
                    <ul>
                      <li>📧 Send order confirmations to external email service</li>
                      <li>💬 Post messages to Slack/Teams when lead is created</li>
                      <li>📦 Sync inventory to external warehouse system</li>
                      <li>💳 Notify payment gateway of invoice status changes</li>
                      <li>📊 Push analytics data to external dashboard</li>
                    </ul>
                  </div>
                </group>
              </page>
            </notebook>
          </sheet>
            <chatter/>
        </form>
      </field>
    </record>

    <record id="view_bitconn_webhook_tree" model="ir.ui.view">
      <field name="name">bitconn.webhook.tree</field>
      <field name="model">bitconn.webhook</field>
      <field name="arch" type="xml">
        <list>
          <field name="name"/>
          <field name="user_id"/>
          <field name="webhook_url"/>
          <field name="can_create"/>
          <field name="can_write"/>
          <field name="can_unlink"/>
        </list>
      </field>
    </record>

    <record id="action_bitconn_webhook" model="ir.actions.act_window">
      <field name="name">Bitconn Webhooks</field>
      <field name="res_model">bitconn.webhook</field>
      <field name="view_mode">list,form</field>
    </record>

    <menuitem id="menu_bitconn_webhook_root" name="Bitconn Webhook" parent="base.menu_custom" sequence="1000"/>
  <menuitem id="menu_bitconn_webhook" name="Webhooks" parent="menu_bitconn_webhook_root" action="action_bitconn_webhook"/>
  </data>
</odoo>
